language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script: py.test home/travis/build/flacode/shopping-guru/tdd_sample/tests/